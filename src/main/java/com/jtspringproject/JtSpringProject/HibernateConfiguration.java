package com.jtspringproject.JtSpringProject;

// ❗ FINAL — Disable Hibernate manual configuration completely
// ❗ Spring Boot JPA will handle DataSource + EntityManager + TransactionManager automatically

public class HibernateConfiguration {
    // Configuration removed intentionally to avoid conflict with Spring Boot JPA auto-config
}
